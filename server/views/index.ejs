<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css'>
    <title>Gold_Level_Project</title>
</head>

<body>
    <header class="container">
        <div id="top-bar" class="d-flex justify-content-between align-items-center py-2">
            <div id="logo">
                <img src="/images/bazaar-high-resolution-logo-transparent.png" class="img-fluid" alt="logo" width="100px" height="50px">
            </div>
            <div id="navBar">
                <button id="menuIcon" class="btn btn-primary" data-bs-toggle="collapse" data-bs-target="#sideMenu">
                    <img src="/icons/menu.png" class="img-fluid" alt="menu logo" width="30px" height="30px">
                </button>
            </div>
        </div>
    </header>
    <hr>
    <div id="searchBar" class="container">
        <div class="input-group mb-3">
            <input type="text" id="search" placeholder="Search" class="form-control">
            <button id="searchButton" class="btn btn-primary">
                <img src="/icons/search-interface-symbol.png" class="img-fluid" alt="menu logo" width="20px" height="20px">
            </button>
        </div>
    </div>

    <div class="collapse" id="sideMenu">
        <div class="container">
            <ul class="list-group mt-3">
                <li class="list-group-item">Home</li>
                <li class="list-group-item">Products</li>
                <li class="list-group-item">About Us</li>
                <li class="list-group-item">Contact us</li>
                <li class="list-group-item">Login/SignUp</li>
            </ul>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

<!-- <script>
    document.addEventListener("DOMContentLoaded", function () {
        const menuIcon = document.getElementById("menuIcon");
        const closeIcon = document.getElementById("closeIcon");
        const sideMenu = document.getElementById("sideMenu");

        const isMenuOpen = localStorage.getItem("isMenuOpen");

        function openSideMenu() {
            sideMenu.style.right = "0";
            menuIcon.classList.add("clicked");
            closeIcon.classList.remove("clicked");
        }
        function closeSideMenu() {
            sideMenu.style.right = "-300px";
            closeIcon.classList.add("clicked");
            menuIcon.classList.remove("clicked");
        }

        menuIcon.addEventListener("click", function () {
            openSideMenu();
            localStorage.setItem("isMenuOpen", "true");
        });

        closeIcon.addEventListener("click", function () {
            closeSideMenu();
            localStorage.setItem("isMenuOpen", "false");
        });

        if (isMenuOpen === "true") {
            openSideMenu();
        } else {
            closeSideMenu();
        }
    });
</script> -->

</html>